# Session Handoff & Review Request — 2026-09-11

> **Người thực hiện:** OpenCode CLI (model `commandcode/deepseek/deepseek-v4.1-flash`)
> **Người nhận:** Claude Code (để phân tích độc lập)
> **Repo:** `github.com/vtgiang-dotcom/Solo-Code-Harness` (nhánh `main`)
> **Trạng thái push:** đã push tới `origin/main`, HEAD = `953ae69`

---

## 0. Vì sao có báo cáo này

Báo cáo viết cho một agent **không có lịch sử hội thoại của session**. Mọi kết luận
đều kèm lệnh đã chạy để có thể tái lập độc lập. Mục tiêu: nhờ Claude Code đánh giá
các quyết định sửa chữa, đặc biệt là các quyết định policy, và chỉ ra chỗ làm sai
hoặc còn sót.

**Session bắt đầu từ đâu:** user yêu cầu "review dự án xem còn gì cần nâng cấp sửa
chữa". Tôi chạy toàn bộ verification gates, đọc các audit trước
(`docs/audit-2026-08-15.md`, `docs/audit-2026-09-04.md`), rồi tổng hợp 10 phát hiện
(F1–F10). User chốt sửa lần lượt F1–F5, rồi F6–F7, rồi F8–F10, rồi commit, rồi push.

---

## 1. Trạng thái gates tại thời điểm bàn giao

Tất cả đo trên máy Windows, Python 3.13.2 (venv).

| Gate | Lệnh | Kết quả |
|---|---|---|
| Pytest | `python -m pytest tools/` | 496 passed, 3 skipped |
| Ruff | `ruff check .` | All checks passed |
| Lint budget | `python tools/check_lint_budget.py` | 72/72 — at budget |
| Secret scan | `python .github/scripts/security_scan.py .` | 858 files, 0 issues |
| Schema | `python tools/validate_schemas.py` | 66 files, 0 errors |
| Garden | `python tools/garden.py` | 0 drift |
| Boundary audit | `python .github/scripts/boundary_audit.py .` | clean (trước là 4 warning) |
| No-skips | `python .github/scripts/check_skips.py tools/` | OK |
| Master checklist | `python .github/scripts/checklist.py .` | 5/5 PASS |

---

## 2. Mười vấn đề đã sửa

### F1 — Version deploy sinh ra sai (Cao)

`tools/deploy.py` đọc `fallback-version` trong `pyproject.toml` để đóng dấu
`.harness.lock`, nhưng giá trị vẫn là `4.1.0` trong khi `agent.yaml` đã lên `4.2.0`.
`tools/garden.py` so sánh `agent.yaml` version với `.harness.lock` version, nên mọi
project vừa deploy xong bị báo drift ngay.

Bằng chứng trước khi sửa — sinh lock vào thư mục tạm bằng chính hàm của deploy:

```
[NEW] .harness.lock — generated (v4.1.0, 54 shared paths)
version = "4.1.0"
```

Sửa: bump `fallback-version` lên `4.2.0`. Sau khi sửa, cùng lệnh in `v4.2.0`.

### F2 — `.harness.lock` gốc ở format cũ (Cao)

Lock gốc chỉ có hai section (`[harness]`, `[boundaries]`), liệt kê `.github`,
`.vscode`, `tools` như harness nguyên khối, và vẫn mô tả engine `jcode` đã retired.
Template chuẩn sinh ra thêm `[shared_files]` và tách `shared_dirs`. `AGENTS.md` dặn
agent đọc `[shared_files]` nhưng mục đó không có. Không test nào kiểm tra lock gốc
(chỉ test lock sinh trong `tmp_path`).

Sửa: viết lại lock — 8 harness dir, 3 shared dir, 69 shared path, 18 file gốc.

### F3 — Template lock lệch khỏi data model deploy (Cao)

`HARNESS_LOCK_TEMPLATE` trong `tools/deploy.py` là chuỗi tĩnh, không ràng buộc gì với
`ROOT_FILES`, `EXCLUSIVE_HARNESS_DIRS`, `SHARED_DIRS`. Hệ quả:

- `.opencode` (cả một engine dir) thiếu khỏi `dirs`.
- `opencode.json`, `eslint.config.js`, `.env.template` thiếu khỏi `files`.

Mọi lock deploy ra đều nói với model rằng ba file đó là code dự án được phép sửa.

Sửa: thêm `.opencode` và ba file. Thêm 4 test ràng buộc template với data model và
lock gốc với `fallback-version` (trong `tools/test_deploy.py`).

### F4 — Tham chiếu jcode còn sót trong file ship (Trung bình)

`jcode` retired ở v4.1.0 (`tools/deploy.py` có entry `RETIRED_ROOT_FILES`), nhưng còn
trong `.env.template` (block FreeModel/OPENAI_* mà không nơi nào trong `tools/` dùng),
`init.sh` (nhãn rulebook), `claude-env.ps1` (comment).

Sửa: gỡ cả ba.

### F5 — File lạc `.opencode/opencode.json` (Trung bình)

Chứa `{"plugin": ["list"]}`, chưa từng được git track, không bị ignore. Config thật
là `opencode.json` ở gốc. Sửa: thêm vào `.gitignore` (không xoá file).

### F6 — Boundary audit báo nhầm skill asset (Thấp) — **quyết định policy**

`.github/scripts/boundary_audit.py` chỉ cho phép tập extension cố định, không có
`.xml`. Skill `mcp-builder` ship `example_evaluation.xml` ở cả 4 mirror engine, nên
audit luôn báo 4 warning.

Sửa: thêm `HARNESS_SKILL_ASSET_EXTENSIONS = {".xml"}`, chỉ chấp nhận khi đường dẫn đi
qua thư mục `skill/` hoặc `skills/`. **Không** thêm `.xml` vào
`HARNESS_ALLOWED_EXTENSIONS` toàn cục, vì làm vậy audit sẽ mù với XML lạc ở bất kỳ
đâu — đúng thứ nó sinh ra để bắt. Kèm `tools/test_boundary_audit.py` (8 test) phủ cả
hai chiều: asset trong skill được phép, XML ngoài skill vẫn bị báo.

### F7 — Lệnh test tài liệu hoá không chạy được (Thấp, môi trường)

`.venv` tạo bằng uv 0.6.6, chỉ chứa một editable install của chính harness, không có
`pytest`. `VIRTUAL_ENV` trỏ vào `.venv` nên `python` phân giải vào đó; `python -m
pytest` báo `No module named pytest`; cả `make test` lẫn `verify.sh` fail. Shim
`pytest.exe` mồ côi của Python 3.10 (không hề được cài) chạy ra exit 1 không output.

Sửa hai phần: cài `pytest` vào `.venv`; khai báo
`[project.optional-dependencies] dev = ["pytest>=8.0", "ruff>=0.6"]` (runtime deps vẫn
rỗng), cập nhật `CONTRIBUTING.md`.

### F8 — Tài liệu `--bare` sai (Thấp)

`claude-env.ps1` đã chuyển full mode thành mặc định mọi profile từ 2026-08-08, giữ
`--bare` làm opt-in. `README.md` và `.env.template` vẫn mô tả gateway profile chạy
qua `--bare`, khiến người đọc tưởng hooks và auto-memory tắt mặc định. Sửa cả hai.

### F9 — Số engine sai trong CONTRIBUTING (Thấp)

`CONTRIBUTING.md` ghi ba engine (OpenCode, Kilo Code, Gemini); thực tế năm (thiếu
Claude Code, GitHub Copilot). Sửa thành năm.

### F10 — Test lifecycle flaky do dùng state thật (Trung bình)

Bốn test trong `tools/test_claude_hooks.py` ghi fixture thẳng vào outbox handoff và
`.solocode/` thật của repo rồi khôi phục thủ công. `.claude/hooks/session_start.py`
phân giải mọi thứ từ `Path.cwd()`, nên khi chạy full suite chúng tranh chấp state dùng
chung: hai test fail ở lần chạy này, pass ở lần sau dù không đổi code.

Sửa: `_run` nhận thêm `cwd`; bốn test dựng fixture dưới `tmp_path`, truyền
`cwd=tmp_path`, bỏ backup/restore thủ công.

Bằng chứng: `test_claude_hooks.py` chạy 3 lần liên tiếp — 40 passed mỗi lần; full
suite 2 lần liên tiếp — 496 passed mỗi lần.

---

## 3. Commits đã push

| Commit | Nội dung |
|---|---|
| `2853a22` | `fix(deploy): sync lock template and version to 4.2.0` |
| `59f1c42` | `chore: drop retired jcode references from shipped config` |
| `5e099a9` | `fix(boundary): scope skill data assets so audit stops crying wolf` |
| `2f9fed1` | `chore(env): drop stale OpenAI-compatible note from .env.template` |
| `92d8467` | `build: declare pytest and ruff as a dev extra` |
| `7873690` | `test(claude): isolate session_start tests from shared repo state` |
| `a7fea4d` | `docs: correct --bare claims to match launcher full-mode default` |
| `01dbc56` | `docs: correct CONTRIBUTING engine count from three to five` |
| `953ae69` | `docs: record 2026-09-11 harness consistency audit and fixes` |

Tài liệu audit đầy đủ: `docs/audit-2026-09-11.md`.

---

## 4. Việc định làm tiếp (chưa làm, chờ quyết định)

### T1 — Ghi policy F6 vào memory (đã hoãn)

Theo `AGENTS.md`, quyết định policy đã chốt nên thêm vào mục `## Decisions` của
`.kilo/memory/MEMORY.md`. Nhưng `tools/garden.py` kiểm tra memory drift, nên phải sửa
đồng thời `.claude/memory/MEMORY.md` và `.copilot/memory/MEMORY.md`, hoặc chạy
`python tools/generate_harness.py --harness claude` rồi copy sang Copilot.

**Lý do hoãn:** chạm vào mirror memory của cả ba engine trong một session đã dài; cần
xác nhận trước khi sửa state dùng chung.

### T2 — Tạo git tag `v4.2.0` (đã hoãn)

Remote mới có tag `v4.1.0` trong khi version nội bộ đã là 4.2.0. Tạo tag là hành động
release, cần yêu cầu riêng.

### T3 — Rà test isolation các file khác

F10 chỉ sửa nhóm flaky đã biết trong `tools/test_claude_hooks.py`. Chưa rà các test
khác có ghi vào đường dẫn thật trong repo hay không.

### T4 — Thêm test ràng buộc lock gốc

Test mới chỉ ràng buộc **template** với data model, và ràng buộc **lock gốc** version
với `fallback-version`. Chưa có test ràng buộc `dirs` **của lock gốc** với
`EXCLUSIVE_HARNESS_DIRS`.

---

## 5. Điểm mở / không chắc chắn — nhờ Claude Code đánh giá

Đây là phần tôi muốn được phân tích kỹ nhất.

### Q1 — `.agents` trong lock gốc nhưng không có trong deploy

Tôi đã thêm `.agents` vào `dirs` của `.harness.lock` gốc (theo bảng boundary trong
`AGENTS.md`), nhưng `DIRS_ALL` trong `tools/deploy.py` **không** có `.agents`, nên
template lock cũng không có. Nghĩa là lock gốc và template hiện khác nhau một mục.

`.agents/` trong repo chỉ chứa `mcp_config.json` (tracked) và một junction
`skills/` (gitignored). Câu hỏi: `.agents` nên là harness dir trong lock gốc không,
hay nên bỏ để lock gốc khớp hoàn toàn với những gì deploy ship? Việc để lệch có vi
phạm nguyên tắc "lock phản ánh thực tế deploy" không?

### Q2 — F6: ngoại lệ theo path shape có phải cách đúng?

Tôi giới hạn `.xml` trong thư mục skill. Lo ngại: một file dự án lạc vào
`skill/` cũng sẽ được tha. Đây có phải đánh đổi hợp lý, hay nên dùng cách chặt hơn
(ví dụ allowlist theo tên file cụ thể, hoặc chỉ cho `scripts/` con của skill)?

### Q3 — F7: cách xử lý đã đúng chưa?

Tôi chọn thêm `dev` extra thay vì thêm `pytest` vào runtime `dependencies`, để giữ
tinh thần stdlib-only mà `CONTRIBUTING.md` tuyên bố. Nhưng tôi cũng đã cài `pytest`
trực tiếp vào `.venv` cục bộ (thay đổi máy, không commit). Việc khai báo extra mà
không có lockfile/CI cài nó có ý nghĩa thực tế không, hay chỉ là tài liệu?

### Q4 — Có bỏ sót vấn đề nào không?

Session này chỉ xoay quanh tính nhất quán giữa file cấu hình, tài liệu và test. Tôi
**chưa** đánh giá: coverage thật của bộ test, chất lượng logic trong các delegate
(`tools/opencode_delegate.py`, `tools/kilo_cli_delegate.py`), tính đúng của các hook
enforcement, hay bất kỳ vấn đề bảo mật nào ngoài secret scan. Nhờ Claude Code rà
thêm hoặc xác nhận đã đủ.

### Q5 — venv dist-info còn version cũ

`.venv` ghi `solo_code_harness-4.1.1.dev0+g853d1d9ac.d20260815` — build từ git ngày
2026-08-15, trước khi repo lên 4.2.0. Đây là artifact local, không nằm trong repo.
Có nên cài lại editable (`pip install -e .`) để version đồng bộ, hay bỏ qua?

---

## 6. Lệnh tái lập toàn bộ

```powershell
# Gates (trên máy này cần trỏ đúng interpreter có pytest)
python -m pytest tools/ -q
ruff check .
python tools/check_lint_budget.py
python .github/scripts/security_scan.py .
python tools/validate_schemas.py
python tools/garden.py
python .github/scripts/boundary_audit.py .
python .github/scripts/check_skips.py tools/
python .github/scripts/checklist.py .
```

Kỳ vọng: 496 passed / 3 skipped; ruff sạch; boundary clean; garden 0 drift;
checklist 5/5.

---

*Bàn giao: 2026-09-11 — HEAD `953ae69` — đã push `origin/main`.*
